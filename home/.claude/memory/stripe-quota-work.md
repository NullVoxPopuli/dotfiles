---
name: stripe-quota-work
description: "In-progress `stripe` branch (Aug 2026) — tiered plans + monthly link quotas; known rough edges"
metadata: 
  node_type: memory
  type: project
  originSessionId: b768c253-bcf8-40e8-a29d-a23c2d531b8a
  modified: 2026-08-11T01:20:23.522Z
---

As of 2026-08-09, branch `stripe` (uncommitted) replaces the binary free-vs-subscribed gate with **tiered plans + monthly link quotas**:

- `api/app/services/plans.ts`: hardcoded PLANS — side-hobby $1/15 links, hobby $5/100, project $15/1000; `NO_SUBSCRIPTION_PLAN` (5), `FREE_PLAN` (unlimited); `planForPriceId()`.
- `api/app/services/link_quota.ts`: `quotaForAccount()` counts links created this UTC month per account.
- Migration adds `(owned_by, created_at)` index on `links`.
- `actions/create.ts` gates on remaining quota (402 "Monthly link limit reached"); `actions/billing.ts` checkout takes a `plan` key, status returns plan/usage/availablePlans.
- Web: new `pricing` route/component, dashboard shows plan+usage, `dashboard.ts` service gains checkout/portal methods.

**Fixed 2026-08-09 (later session):** billing.ts misnested attributes (plan/usage/availablePlans were inside `stripe`); dashboard route never called `dashboard.refresh()`; `{{#if ...usage.remaining}}` treated 0 as "Unlimited". Dashboard component rewritten (usage meter, links table, billing-portal button). Also fixed: SSR home routes in `api/start/routes.ts` needed `.domain(DOMAIN)` — Adonis only falls back to domainless routes when NO registered domain matches the hostname.

**WarpDrive migration (2026-08-09):** web-client data layer now uses WarpDrive 5.8 — `app/data/{types,schemas,requests,billing}.ts` (branded types, `withDefaults` schemas, `withReactiveResponse` builders, plain-fetch Stripe redirect actions), schemas registered in `services/store.ts`, `import '@warp-drive/ember/install'` in app.ts (required, else "Signal hooks not configured"). DashboardService deleted: routes return `{ billing: store.request(...) }` (futures MUST be wrapped in an object — Ember awaits thenables returned from `model()`), components render via `<Request>` named blocks. API `jsonapi/link.ts` fixed to valid JSON:API (`type`, `relationships.*.data`). Pricing cents-as-dollars bug fixed via `formatPrice`.

Home-page form now creates links via a `createLink` builder (`op: 'createRecord'`); read builders use `op: 'query'` + `cacheOptions.types` so the DefaultCachePolicy auto-invalidates dashboard queries after a create (verified live).

Visit tracking (2026-08-09): redirect controller atomically increments `links.visits` AND inserts a `link_visits` row (visited_at, referrer, user_agent; indexed on link_id+visited_at) — per-visit rows are the substrate for the pricing page's promised basic/advanced click stats, which have no UI yet. Pricing page has plan feature lists (client-side `PLAN_FEATURES` map), a Custom tier (mailto sales@nvp.gg — invented address, unconfirmed), and a Free row with cancel-via-portal.

link_quota.ts now uses a COUNT query (`.count('* as total')` + `$extras.total`).

Link management page (`/dashboard/links`, 2026-08-09): create form w/ quota gating + delete w/ confirm; refresh after mutations via `<Request>` content-features (`billingState.refresh`/`linksState.refresh`) since DELETE's 204-style empty doc can't drive type-based invalidation. Fixed along the way: `deleteLink` action had NO auth (anyone could delete any link — now scoped via `visibleTo`); `jsonapi.empty()` returned invalid `{data:{}}` (now `{data:null}` — JSONAPICache rejects id-less resources).

Public authenticated endpoints (2026-08-09, pushed): GET /v1/accounts/:id, /v1/users/:id (scoped to caller's account, 404 otherwise), /v1/links/:id/visits. API payloads are compound documents: relationships carry `data` + `links.related` AND the related resources are `included` — WarpDrive linksMode belongsTo REQUIRES sync (`async: false`) + included data (async linksMode unsupported; sync without included throws). Docs = hand-authored `swag.ts` → `node ace swagger:generate` → public/swagger.json (swagger.yml is a dead leftover).

Team/multi-account (2026-08-10, pushed): `account_memberships` pivot (role admin/member; `users.account_id` = ACTIVE account pointer, memberships = belonging); `account_invitations` (uuid token, 7-day expiry, shareable `/join/:token` links — no email); `custom_domains` (plan-gated: hobby 2/project 3/free unlimited; links get nullable `domain`, redirect scoped by Host, domainless fallback route group for custom hosts); `users.is_staff` + `node ace plan:set <account> <plan>` (bypasses Stripe via `overridePlan`); github id 199018 → staff + glimdown admin membership at login (`ensureSpecialGrants`); account switcher = select in shell header → POST /v1/me/account → full page reload (kills stale cache). Plans now carry `teammates` + `customDomains` limits. Team docs sideload FULL linkage chains (membership→user+account→admin) because linksMode demands it.

PR-review redesign (2026-08-10, pushed): account switching is URL-only — dashboard lives at `/{8-char-account-slug}/...` (client resolves slug→full id via memberships, canonicalizes full-uuid URLs), account-scoped API endpoints take `?account=<full-id>` (membership-checked via `#services/account_context`, default = personal); `users.account_id` now permanently means PERSONAL account (no switch endpoint, no DB writes); per-account URLs give per-account cache keys (no reload on switch). `accounts.is_personal` + POST /v1/accounts (additional accounts gated by personal plan: side-hobby 1/hobby 2/project 3/none 0/staff unlimited). Deletes 404 on missing (Preston's preference — no silent no-ops). Account/user visibility = shared membership. CI lint = `pnpm lint:fix && git diff --exit-code` (prettier drift fails CI — run lint:fix before committing).

API keys (2026-08-10, pushed): Adonis access tokens with tokenable = `account_membership` (table `api_keys`, prefix `nvp_`, CASCADE revokes on membership removal); the `api` guard authenticates memberships now (User.accessTokens provider removed; manager-level `auth.authenticate()` calls became `auth.use('web')` everywhere because the guards' user types diverged). Quota is account-wide from the plan: hobby 1, project 3, others 0, FREE_PLAN null; expired keys don't count. Management endpoints (GET/POST `/v1/api-keys`, DELETE `/v1/api-keys/:id`) are session-only; links endpoints accept `Authorization: Bearer` with scopes `links:read`/`links:write`, keys 404 on foreign `?account=`; billing became session-only. Web: `/{account}/api-keys` route (api-key-manager/-table/new-api-key under routes/dashboard/api-keys/), one-time secret banner. Behavior change ridden along: POST /v1/links with a non-membership `?account=` now 404s (was 402), and its 402 detail is always 'Monthly link limit reached'. Dev note: personal account was put on the `project` plan via `node ace plan:set` for testing, and a real 90-day key "CI deploys" exists in the dev DB.

jsonapi-adonis migration (2026-08-10, MERGED into `stripe`, commits 58bb742/07afa18/f030e39): @evoactivity/jsonapi-adonis 0.2.0 (Liam Potter) now builds every Lucid-backed document — app/jsonapi/ keeps only index.ts (error helpers/send) + api_key.ts. Seven resource classes in app/resources/ (singular `static type` pins client-facing types; curated attributes stop oauth-column leaks — User oauth cols have NO serializeAs:null). Contract changes (Preston OK'd, "0 API users"): `?accountId=` replaces `?account=` (spec reserves lowercase params), writes are JSON:API documents ({data:{type,attributes}} — flat bodies 400; invitations/accept + api-keys + billing stay flat RPC bodies), `included` is ?include= driven (client builders send e.g. include=ownedBy,createdBy / user,account.admin). KEY GOTCHA: WarpDrive JSONAPICache dev-mode validates that EVERY linksMode belongsTo (primary AND included) carries links.related → all serving routes must be named `<type>.<action>` (unnamed serving route ⇒ zero links in the doc) and RelatedController serves GET /<collection>/:id/:relation (account/user/membership/invitation/domain, per-resource visibility). SSR home form posts flat form data into createLinkFromValues (shared with the deserializing API entry). forceMimeType kept (library's negotiation middleware doesn't REQUIRE the vnd.api+json mime). Dev note: `node ace serve --hmr` dies (503, no auto-restart) on adonisrc.ts/provider changes — full restart needed; cookie sessions survive, GitHub OAuth re-login is silent.

Error layer fully on jsonapi-adonis (2026-08-10, commit 02ea095): app/jsonapi/index.ts DELETED — errors are thrown JsonApiExceptions (sugar in `#exceptions/api_errors`: notFound/notAuthenticated/notAuthorized/unprocessable/paymentRequired) rendered by toErrorDocument in the action wrappers; `#jsonapi` root import mapping removed (only #jsonapi/api_key remains). Error docs are spec-shaped: `status` is a STRING ('401'), messages in `detail` under generic titles ('Unauthorized', 'Unprocessable Content'); 402 quota titles unchanged. KEY GOTCHA #2: WarpDrive's store.request() ALWAYS hydrates (EnableHydration set by the store itself) → bodyless 204s can never work through it (CacheHandler asserts "expected response content") — deletes return 200 + `jsonApi.render(null)`. The SSR error.edge view matches on error.status === "401" and prints detail||title. htmlAction/action wrappers take callbacks returning unknown; toErrorDocument also maps E_UNAUTHORIZED_ACCESS (any http-shaped error) → no special-casing.

Account switcher (2026-08-10, commit c94244f): nvp.ui Menu at the top of the sidebar (`routes/dashboard/account-switcher.gts`) — trigger shows active account, LinkItems navigate to /{slug} with aria-current, single-membership renders a plain name. `<PortalTargets />` (ember-primitives) now renders in the application template — menus/popovers need it. nvp.ui PR #115 (repo cloned at ~/Development/NullVoxPopuli/nvp.ui, branch menu-bare-variant-and-portaled-hints) fixes the ESC-hint clipping (hint now portals with the content; inline it broke under ApplicationShell's container-query layout containment) and adds `@variant="bare"` to Menu/Button + an account-switcher docs example. DONE (0.10.0 released, commit 3606fc0): nvp.ui bumped, switcher Menu uses `@variant="bare"`, ESC-hint verified attached to the menu. Gotcha hit on the way: Vite's dep prebundle cache (`web-client/node_modules/.vite`) served the OLD nvp.ui after the bump — the running vite server must be restarted (cache cleared) for a dep upgrade to take effect. NOTE: dev DB was reset ~Aug 10 — Preston's personal account is now 15acce21-… (user id aedd5900-…), glimdown membership intact, no plan override applied.

**Still rough** (verify before mentioning): Vite HMR websocket fails through the Apache proxy (no Upgrade forwarding); deleting a link frees its quota slot (`quotaForAccount` counts surviving rows) — create/delete cycling bypasses the monthly cap.

**Dev serving note:** user's commit 777180a (2026-08-09) made dev origins portless — Apache on :80 proxies `nvp.local`→api:5001, `app.nvp.local`→vite:5002. Browse the app at `http://app.nvp.local/` (no port), else CORS blocks `/_/auth/me` (allowlist is exact-match portless origins).

Related: [[url-shortener-stack]]
