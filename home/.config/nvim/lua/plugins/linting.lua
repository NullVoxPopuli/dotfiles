-- Linters don't all have language server plugins,
-- otherwise I'd use the lsp integration
return function (use)
  -- Couldn't get it working... which is why we like LSPs
  -- use { "mfussenegger/nvim-lint" }
end
