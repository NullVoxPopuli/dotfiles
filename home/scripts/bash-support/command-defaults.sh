# Requires exa https://the.exa.website/
# sudo apt install exa
#
# to use old ls, /usr/bin/ls -- which ls shows this
alias ls='exa -h'
alias la="exa -lah"

# Requires bat https://github.com/sharkdp/bat
# sudo apt install bat
#
# to use old cat, /usr/bin/cat -- which cat shows this
alias cat="batcat"
